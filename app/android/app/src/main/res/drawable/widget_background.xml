<?xml version="1.0" encoding="utf-8"?>
<!-- A plain rounded card. Deliberately not themed from the app's dynamic
     colour: a widget is drawn by the launcher's process, which has no access to
     the app's Material state, and guessing would look wrong on half of them. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?android:attr/colorBackground" />
    <corners android:radius="16dp" />
</shape>
