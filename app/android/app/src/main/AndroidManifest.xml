<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Metadata search + cover downloads (release builds need this explicitly). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- ISBN barcode scanning (plan 5 #16). Requested at first use by
         mobile_scanner; the feature falls back to typing an ISBN when it is
         denied, so the camera stays optional. `required="false"` keeps the app
         installable on a device without one. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Borrowing news in the status bar, opt-in in Preferences. Android 13+
         also requires this at runtime; see NotificationTray.requestPermission,
         which is why the preference cannot be switched on without asking. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <application
        android:label="Vellum"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/full_backup_content"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Launcher long-press shortcuts (plan 5 #40). Static: scanning,
                 adding and continuing are always meaningful, and a dynamic
                 shortcut would need a Kotlin service keeping in step with a
                 library it can't read. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
            <!-- Open-with / share-target import (plan 5 #20): the natural way to
                 get a book into Vellum from Files, Gmail, or a chat app.
                 VIEW handles "Open with", SEND(_MULTIPLE) handles "Share".
                 Both mime type and a .pdf/.epub path pattern are matched
                 because plenty of senders hand over a generic
                 application/octet-stream. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:mimeType="application/pdf"/>
                <data android:mimeType="application/epub+zip"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/pdf"/>
                <data android:mimeType="application/epub+zip"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/pdf"/>
                <data android:mimeType="application/epub+zip"/>
            </intent-filter>
        </activity>

        <!-- The continue-reading widget (plan 5 #40). It reads a snapshot the
             app pushed through home_widget and never touches the database —
             a second writer to the app's SQLite file would be far worse than a
             stale widget. -->
        <receiver
            android:name=".ContinueReadingWidget"
            android:label="@string/widget_name"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/continue_reading_widget_info" />
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
